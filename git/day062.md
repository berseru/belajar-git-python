# 🐍 Day 62 – List Comprehension

---

## 📋 Membuat List dengan Cepat

```python
angka = [x for x in range(5)]
print(angka)  # Output: [0, 1, 2, 3, 4]
```

## 🎯 Dengan Syarat

```python
angka_genap = [x for x in range(10) if x % 2 == 0]
```

---
